1) все клики по кнопкам/ссылкам, приводящие к действиям API, поднимаются до рута приложухи типа $emit('methodName', data), где проверяется флаг isAppBusy и выставляются классы is-loading/is-static элементам интерфейса

2) маплю vuex-геттеры в computed и vuex-действия в методы приложухи  работаю с ними как со свойствами корневого компонента

3) во vuex будет несколько разделов - dogs, trustees, payments и global, ui (общие для всего приложения данные типа флагов isAppBusy, isLoggedIn, ключа сессии)

4) выделять в отдельные компоненты элементы типа кнопок и всё, что может быть как-то повторно использовано

5) получение списков данных/данных по id - где вызывать action?
